# Notes on Claude stow package

The contents of the `.claude` folder is mostly symlinked from `dotfiles/.agents`.

A bit different from other harnesses, the global Claude context is `CLAUDE.md`, which is symlinked from `dotfiles/.agents/AGENTS.md`.
